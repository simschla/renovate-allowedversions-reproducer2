# renovate-allowedversions-reproducer

Currently, renovate creates PR for a guava-android version ([PR#1](pull/1)) but should only allow versions ending in `-jre`. This is a minimal reproducer image.
